diff --git a/sound/soc/codecs/wcd938x.c b/sound/soc/codecs/wcd938x.c
index c69e18667..5972b8048 100644
--- a/sound/soc/codecs/wcd938x.c
+++ b/sound/soc/codecs/wcd938x.c
@@ -1173,6 +1173,8 @@ static int wcd938x_codec_enable_adc(struct snd_soc_dapm_widget *w,
 	struct snd_soc_component *component = snd_soc_dapm_to_component(w->dapm);
 	struct wcd938x_priv *wcd938x = snd_soc_component_get_drvdata(component);
 
+	dev_info(component->dev, "kebab-mic-trace: wcd938x_codec_enable_adc widget=%s shift=%d event=%d\n", w->name, w->shift, event);
+
 	switch (event) {
 	case SND_SOC_DAPM_PRE_PMU:
 		snd_soc_component_write_field(component,
@@ -1435,6 +1437,8 @@ static int wcd938x_codec_enable_micbias(struct snd_soc_dapm_widget *w,
 	struct snd_soc_component *component = snd_soc_dapm_to_component(w->dapm);
 	int micb_num = w->shift;
 
+	dev_info(component->dev, "kebab-mic-trace: wcd938x_codec_enable_micbias widget=%s micb_num=%d event=%d\n", w->name, micb_num, event);
+
 	switch (event) {
 	case SND_SOC_DAPM_PRE_PMU:
 		wcd938x_micbias_control(component, micb_num, MICB_ENABLE, true);
